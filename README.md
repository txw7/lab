# Formal work lab

This repository combines three source directories:

- `formal/`: formal verification and proof tooling from `FORMAL/`.
- `topology-workbench/`: the topology formal workbench.
- `sim-lab/`: the Rust simulation lab crate.
